# description of internal App state
connection: # should/may be saved in cookies (ideally it should be encrypted)
  - name: first_server
    url: test.com
    port: 8086
    user: user
    password: pass
    isSelected: true # is currently selected
  - name: second_server
    url: test.com
    port: 8086
    user: user
    password: pass
    isSelected: false

# queried dynamically after connecting to selected server
database:
  first_server:
    # TODO: this should be refreshed somehow
    db1:
      measurement1:
        fields:
          field1:
            name: Field1
            type: string
          field2:
            name: Field1
            type: string
      measurement2:
    db2:
      ...

  second_server:
    db1:
      - measurement1
      - measurement2
    db2:
      ...

connection: # current connection with Influx
  activeServer: first_server # currently connected/selected server

ui:
  isSidebarOpen: false,
  navMenu: [],
  modal:
    isOpen: false,
    title: '',
    content: ''

  influx: {
    data: {},
    isQuerying: false,
    error: false,
    queryParams: {
      query: '',
      host: '',
      username: '',
      password: ''
    }
  },
  router: {
    location: {
      pathname: '/',
      search: '',
      hash: ''
    },
    action: 'POP'
  }
}
